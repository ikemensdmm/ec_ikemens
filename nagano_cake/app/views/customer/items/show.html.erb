<%= attachment_image_tag @item, :image, size: "340x220", fallback: "no_image.jpg" %>
<h3><%= @item.name %></h3>
<p><%= @item.description %></p>
<% if @item.is_status == true %>
  <h4>¥<%= (@item.price_tax).to_s(:delimited, delimiter: ',') %></h4>
  <p>(税込)</p>
  <%= form_with model:@cart_item, url:customer_cart_items_path, method: :post do |f| %>
    <%= f.hidden_field :item_id, value: @item.id %>
    <%= f.select :quantity, (1..10).to_a, include_blank: "個数選択" %>
    <%= f.submit "カートに入れる", class: "btn btn-success" %>
  <% end %>
<% else %>
  <h4>売り切れ</h4>
<% end %>